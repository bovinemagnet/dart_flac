name: dart_flac
description: A Dart implementation of the FLAC (Free Lossless Audio Codec) library for reading and decoding FLAC audio files.
version: 0.1.0
repository: https://github.com/bovinemagnet/dart_flac

environment:
  sdk: ^3.0.0

dependencies:
  crypto: ^3.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
